@AGENTS.md

# Claude Code adapter

- `AGENTS.md` is the shared cross-platform agent protocol for this repository.
- For any interface, visual, UX, interaction, or sound-tool task, follow its required design workflow before editing.
- Read `FIELD_SPECIMEN_STYLE_GUIDE.md` completely when `AGENTS.md` requires it; do not import the full guide into every unrelated session.
- When using Claude Code subagents, include the Multi-agent handoff instruction from `AGENTS.md` in each visual task.
- Do not duplicate or rewrite these rules in auto memory. Propose changes to the tracked canonical files instead.
